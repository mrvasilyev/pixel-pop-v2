[phases.setup]
nixPkgs = ["python3"]

[phases.install]
cmds = ["python -m venv .venv", ". .venv/bin/activate", "pip install -r requirements.txt"]

[start]
cmd = ". .venv/bin/activate && pip list && uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
